{{- if .Values.spec.managed.aws.machinePoolTemplate }}
---
apiVersion: cluster.x-k8s.io/v1beta1
kind: MachinePool
metadata:
  name: {{ include "kubedbcom-mongodb-editor-options.fullname" . }}
  namespace: {{ .Values.spec.managed.clusterNamespace }}
spec:
  clusterName: {{.Values.spec.managed.clusterName}}
  {{- if eq .Values.spec.mode "Replicaset" }}
  replicas: {{.Values.spec.replicaSet.replicas}}
  {{- else if eq .Values.spec.mode "Standalone" }}
  replicas: 1
  {{- end }}
  template:
    spec:
      bootstrap:
        dataSecretName: ""
      clusterName: {{.Values.spec.managed.clusterName}}
      infrastructureRef:
        apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
        kind: AWSManagedMachinePool
        name: {{ include "kubedbcom-mongodb-editor-options.fullname" . }}
---
apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: AWSManagedMachinePool
metadata:
  name: {{ include "kubedbcom-mongodb-editor-options.fullname" . }}
  namespace: {{ .Values.spec.managed.clusterNamespace }}
spec:
  eksNodegroupName: {{.Values.spec.managed.aws.machinePoolTemplate.name}}
{{- end }}
