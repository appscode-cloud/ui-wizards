elements:
  - label:
      text: Dedicated?
    type: switch
    schema:
      $ref: schema#/properties/dedicated
  - label: 
      text: Zones
    if: ifDedicated
    type: multiselect
    schema:
      $ref: schema#/properties/zones
    options: 
      - zone 1
      - zone 2
      - zone 3   
  - label: 
      text: SKU
    type: input
    schema:
      $ref: schema#/properties/sku
label:
  text: Capi-Provider
show_label: true
type: single-step-form
