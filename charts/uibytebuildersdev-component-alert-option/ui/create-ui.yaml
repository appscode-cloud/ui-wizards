elements:
- label:
    text: labels.alert_enable_question
  schema:
    $ref: schema#/properties/enabled
  type: switch
- if: showAlertSection
  isArray: true
  keys:
    label:
      text: labels.label.key
  label:
    text: labels.label.label
  schema:
    $ref: schema#/properties/labels
  type: key-value-input-form
  values:
    label:
      text: labels.label.value
    schema:
      $ref: schema#/properties/labels/additionalProperties
    type: input
label:
  text: labels.alert_profile
show_label: true
schema:
  $ref: schema#/
type: single-step-form