{{- if .Values.spec.configuration }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kubedbcom-weaviate-editor-options.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubedbcom-weaviate-editor-options.labels" . | nindent 4 }}
data:
  conf.yaml: {{ .Values.spec.configuration | b64enc | quote }}
{{- end }}
