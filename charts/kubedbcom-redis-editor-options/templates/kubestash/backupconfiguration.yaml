{{- if eq .Values.spec.backup.tool "KubeStash" -}}

apiVersion: core.kubestash.com/v1alpha1
kind: BackupConfiguration
metadata:
  name: {{ include "kubedbcom-redis-editor-options.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubedbcom-redis-editor-options.labels" . | nindent 4 }}
spec:
  target:
    apiGroup: kubedb.com
    kind: Redis
    name: {{ include "kubedbcom-redis-editor-options.fullname" . }}
    namespace: {{ .Release.Namespace }}
  backends:
    - name: redis-backend
      storageRef:
        name: {{ .Values.spec.backup.kubestash.storageRef.name }}
        namespace: {{ .Values.spec.backup.kubestash.storageRef.namespace }}
      retentionPolicy:
        name: {{ .Values.spec.backup.kubestash.retentionPolicy.name }}
        namespace: {{ .Values.spec.backup.kubestash.retentionPolicy.namespace }}
  sessions:
    - name: redis-frequent-backup
      sessionHistoryLimit: 2
      scheduler:
        schedule: {{ .Values.spec.backup.kubestash.schedule }}
        jobTemplate:
          backoffLimit: 1
      repositories:
        - name: redis-repo
          backend: redis-backend
          directory: /redis-repo
          encryptionSecret:
            name: {{ .Values.spec.backup.kubestash.encryptionSecret.name }}
            namespace: {{ .Values.spec.backup.kubestash.encryptionSecret.namespace }}
      addon:
        name: redis-addon
        tasks:
          - name: logical-backup
        jobTemplate:
          spec:
            securityContext:
              runAsUser: 0
              runAsGroup: 0
{{- end }}
