en:
  labels:
    alert:
      name: Alert
      toggle: Can user modify Alert?
    archiver:
      name: Archiver
      default: Default Archiver
      toggle: Can user modify Archiver?
    backup:
      name: Backup
      default: Enable Backup
      toggle: Can user modify Backup?
    clusterTier:
      name: Cluster Tier
      nodeTopology:
        availableNodeTopology: Available Node Topologies
        default: Default Node Topology
        name: Node Topology
        toggle: Can user modify Node Topology?
      placement:
        availablePlacement: Available Placements
        default: Default Placement 
        name: Placement Policy
        toggle: Can user modify Placement?
      toggle: Can users modify Cluster Tier?
    clusterIssuers:
      available: Available Cluster Issuers
      default: Default Cluster Issuer
      name: Cluster Issuers
      toggle: Can user modify Cluster Issuers?
    databases:
      ClickHouse:
        configure: Configure ClickHouse
        toggle: Can user modify ClickHouse Version?
        toggleMode: Can user modify ClickHouse Mode?
      Druid:
        configure: Configure Druid
        toggle: Can user modify Druid Version?
        toggleMode: Can user modify Druid Mode?
      default: Default Version
      defaultMode: Default Mode
      Elasticsearch:
        configure: Configure Elasticsearch
        toggle: Can user modify Elasticsearch Version?
        toggleMode: Can user modify Elasticsearch Mode?
      FerretDB:
        configure: Configure FerretDB
        toggle: Can user modify FerretDB Version?
        toggleMode: Can user modify FerretDB Mode?
      Hazelcast:
        configure: Configure Hazelcast
        toggle: Can user modify Hazelcast Version?
        toggleMode: Can user modify Hazelcast Mode?
      Ignite:
        configure: Configure Ignite
        toggle: Can user modify Ignite Version?
        toggleMode: Can user modify Ignite Mode?
      Oracle:
        configure: Configure Oracle
        toggle: Can user modify Oracle Version?
        toggleMode: Can user modify Oracle Mode?
      Cassandra:
        configure: Configure Cassandra
        toggle: Can user modify Cassandra Version?
        toggleMode: Can user modify Cassandra Mode?
      Kafka:
        configure: Configure Kafka
        toggle: Can user modify Kafka Version?
        toggleMode: Can user modify Kafka Mode?
      MSSQLServer:
        configure: Configure MSSQLServer
        toggle: Can user modify MSSQLServer Version?
        toggleMode: Can user modify MSSQLServer Mode?
      MariaDB:
        configure: Configure MariaDB
        toggle: Can user modify MariaDB Version?
        toggleMode: Can user modify MariaDB Mode?
      Memcached:
        configure: Configure Memcached
        toggle: Can user modify Memcached Version?
        toggleMode: Can user modify Memcached Mode?
      modes: Available Modes
      MongoDB:
        configure: Configure MongoDB
        toggle: Can user modify MongoDB Version?
        toggleMode: Can user modify MongoDB Mode?
      MySQL:
        configure: Configure MySQL
        toggle: Can user modify MySQL Version?
        toggleMode: Can user modify MySQL Mode?
      name: Configure Databases
      PerconaXtraDB:
        configure: Configure PerconaXtraDB
        toggle: Can user modify PerconaXtraDB Version?
        toggleMode: Can user modify PerconaXtraDB Mode?
      PgBouncer:
        configure: Configure PgBouncer
        toggle: Can user modify PgBouncer Version?
        toggleMode: Can user modify PgBouncer Mode?
      Pgpool:
        configure: Configure Pgpool
        toggle: Can user modify Pgpool Version?
        toggleMode: Can user modify Pgpool Mode?
      Postgres:
        configure: Configure Postgres
        toggle: Can user modify Postgres Version?
        toggleMode: Can user modify Postgres Mode?
      ProxySQL:
        configure: Configure ProxySQL
        toggle: Can user modify ProxySQL Version?
        toggleMode: Can user modify ProxySQL Mode?
      RabbitMQ:
        configure: Configure RabbitMQ
        toggle: Can user modify RabbitMQ Version?
        toggleMode: Can user modify RabbitMQ Mode?
      Redis:
        configure: Configure Redis
        toggle: Can user modify Redis Version?
        toggleMode: Can user modify Redis Mode?
      Singlestore:
        configure: Configure Singlestore
        toggle: Can user modify Singlestore Version?
        toggleMode: Can user modify Singlestore Mode?
      Solr:
        configure: Configure Solr
        toggle: Can user modify Solr configuration?
        toggleMode: Can user modify Solr Mode?
      ZooKeeper:
        configure: Configure ZooKeeper
        toggle: Can user modify ZooKeeper Version?
        toggleMode: Can user modify ZooKeeper Mode?
      versions: Available Versions 
    deletionPolicy:
      name: Deletion Policy
      default: Default Deletion Policy
      toggle: Can user modify Deletion Policy?
    deployment:
      dedicated: For production applications with sophisticated workload requirements.
      name: Deployment Type
      shared: For exploring databases when high-performance is not required.
      toggle: Can users modify Deployment type?
    leftPanel:
      name: Left Panel
      showBackup: Show Backup?
      showBackupLegacy: Show BackupLegacy?
      showInsights: Show Insights?
      showOperations: Show Operations?
      showSecurity: Show Security?
      showVaultInfo: Show VaultInfo?
    nodeSelector:
      key: Key
      label: Node Selector
      value: Value
    pointInTimeRecovery:
      name: Point In Time-Recovery
      default: Enable Point In-Time Recovery
      toggle: Can users modify Point In-Time Recovery?
    storageClass:
      name: Storage Class
      available: Available Storage Classes
      default: Default Storage Class
      toggle: Can users modify Storage Class?
    showPreview:
      name: Preview
      toggle: Show Preview?
    tls:
      default: Enable TLS?
      name: TLS
      toggle: Can users modify TLS?
    expose:
      default: Expose via gateway?
      name: Expose
      toggle: Can users modify Exposer via gateway?
    monitoring:
      name: Monitoring
      toggle: Can users modify Monitoring?
      agent: Agent
      exporter:
        name: Resources
      serviceMonitor:
        name: Labels
        value: Value
        key: Key
    tolerations:
      label: Tolerations
      effect: Effect
      key: Key
      operator: Operator
      tolerationSeconds: Toleration Seconds
      value: Value
    usePrefix: Storage Prefix
  options:
    deletionPolicy:
      delete:
        description: Keep only database Secrets and backed up data.
        label: Delete ( Keep only database Secrets and backed up data )
      doNotTerminate:
        description: Prevent deletion of the Druid CRD.
        label: DoNotTerminate ( Prevent deletion of the Druid CRD )
      halt:
        description: 'Keep PVCs, database Secrets and backed up data.'
        label: Halt ( Keep PVCs, database Secrets and backed up data )
      wipeOut:
        description: Delete everything including backed up data.
        label: WipeOut ( Delete everything including backed up data )