Get the Stash BackupConfiguration by running the following command:

  kubectl --namespace {{ .Release.Namespace }} get validatingwebhookconfigurations.admissionregistration.k8s.io {{ .Release.Name }}
